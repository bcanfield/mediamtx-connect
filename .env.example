# MediaMTX Connect Environment Variables
# Copy this file to .env and customize as needed

# ===========================================
# Database
# ===========================================

# SQLite database URL (relative to project root for local dev)
DATABASE_URL="file:./src/lib/prisma/database.db"

# ===========================================
# Docker Compose Variables
# ===========================================

# Directory where MediaMTX stores recordings (used in docker-compose.yml)
# This path must be accessible by both the mediamtx and mediamtx-connect containers
MEDIAMTX_RECORDINGS_DIR=./recordings

# ===========================================
# Development/Seed Variables (optional)
# ===========================================
# These are used by prisma/seed.ts for local development
# If not set, defaults will be used

# MediaMTX server URL (internal Docker network or localhost)
# BACKEND_SERVER_MEDIAMTX_URL=http://localhost

# MediaMTX API port
# MEDIAMTX_API_PORT=9997

# External URL for browser access to MediaMTX streams
# REMOTE_MEDIAMTX_URL=http://localhost

# Path to recordings directory
# MEDIAMTX_RECORDINGS_DIR=./test-data/recordings

# Path to screenshots directory
# MEDIAMTX_SCREENSHOTS_DIR=./test-data/screenshots

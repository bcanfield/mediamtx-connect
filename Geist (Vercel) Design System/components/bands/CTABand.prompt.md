The end-of-page call-to-action band — centered display headline and a marketing pill, on plain canvas with a top hairline.

```jsx
<CTABand title="Ready to deploy?" subtitle="Start building on Vercel in seconds."
  actions={<Button variant="primary" size="lg">Start Deploying</Button>} />
```
